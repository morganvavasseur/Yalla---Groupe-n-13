{# app/Resources/views/admin/fonctions_redacteurs.html.twig #}

{{ include('admin/header.html.twig')}}

{% block body %}

{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
{{ include('admin/nav.html.twig')}}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header" data-background-color="orange">
                        <h1 class="title h1">Fonctions Rédacteurs</h1>
                    </div>
                    <div class="card-content table-responsive">
                        <table class="table table-orange">
                            <thead class="text-primary">
                                <tr>
                                    <th>#</th>
                                    <th>Fonction</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for fonction in fonctions %}
                                    <tr>
                                        <td>{{ fonction.idFonction }}</td>
                                        <td>{{ fonction.nomFonction }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                        <div class="col-sm-4">
                            <div class="form-group label-floating">
                                <a href="{{ path("admin ajout fonction") }}" class="btn btn-warning">Ajouter</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% endblock %}

{{ include('admin/footer.html.twig')}}