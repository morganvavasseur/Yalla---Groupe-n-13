{# app/Resources/views/fonctions_redacteurs.html.twig #}

{{ include('header.html.twig')}}

{% block body %}

{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
{{ include('nav.html.twig')}}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header" data-background-color="orange">
                        <h1 class="title h1">Fonctions Rédacteurs</h1>
                    </div>
                    <div class="card-content table-responsive">
                        <table class="table">
                            <thead class="text-primary">
                                <tr>
                                    <th>#</th>
                                    <th>Fonction</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for fonction in Fonction %}
                                    <tr>
                                        <td>{{ fonction.id }}</td>
                                        <td>{{ fonction.nomFonction }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% endblock %}

{{ include('footer.html.twig')}}