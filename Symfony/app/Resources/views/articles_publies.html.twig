{# app/Resources/views/articles_publies.html.twig #}

{{ include('header.html.twig')}}

{% block body %}

{{ include('nav.html.twig')}}

{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header" data-background-color="orange">
                        <h1 class="title h1">Articles publiés</h1>
                    </div>
                    <div class="card-content table-responsive">
                        <table>
                            <tr>
                                <thead class="text-primary">
                                <th>Titre</th>
                                <th>Catégorie</th>
                                <th>Rédacteur</th>
                                <th>Date de publication</th>
                                <th>Version</th>
                                <th>Modifier</th>
                                <th>Supprimer</th>
                                </thead>
                                <tbody>

                                </tr>
                                {% for article in articles %}
                                    <tr>
                                        <td><a href="/article/{{ article.slugArticle }}">{{ article.titreArticle }}</a></td>
                                        <td>{{ article.idCategorie.nomCategorie }}</td>
                                        <td>{{ article.idRedacteur.prenomRedacteur }} {{ article.idRedacteur.nomRedacteur }}</td>
                                        <td>{{ article.dateArticle.format('j/m/Y, H:m:s')}}</td>
                                        <td>{{ article.versionArticle }}</td>
                                        <td><a href="/article/modifier/{{ article.slugArticle }}" class="btn btn-warning">M</a></td>
                                        <td><a href="/article/supprimer/{{ article.slugArticle }}" class="btn btn-danger">X</a></td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                        </table>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

{% endblock %}

{{ include('footer.html.twig')}}